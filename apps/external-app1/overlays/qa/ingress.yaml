# infrastructure-repo/apps/external-app1/overlays/qa/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: external-app1
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - external-app1-qa.gitops.local
    secretName: external-app1-qa-tls
  rules:
  - host: external-app1-qa.gitops.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: external-app1
            port:
              name: http
