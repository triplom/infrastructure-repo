# infrastructure-repo/apps/external-app2/overlays/dev/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: external-app2
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-staging" # Use staging for testing, then switch to prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - external-app2-dev.gitops.local
    secretName: external-app2-dev-tls
  rules:
  - host: external-app2-dev.gitops.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: external-app2
            port:
              number: 80
