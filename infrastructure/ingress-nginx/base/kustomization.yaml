apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ingress-nginx is deployed via Helm (see deploy.sh and values.yaml).
# No Kustomize-managed resources in this directory.
resources: []
