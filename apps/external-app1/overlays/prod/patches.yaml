apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-app1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: external-app1
  template:
    metadata:
      labels:
        app: external-app1
    spec:
      imagePullSecrets:
      - name: github-registry-secret
      containers:
      - name: external-app1
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: LOG_LEVEL
          value: "warn"
        resources:
          limits:
            memory: "512Mi"
            cpu: "1000m"
          requests:
            memory: "256Mi"
            cpu: "500m"
