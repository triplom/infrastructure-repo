apiVersion: v1
kind: Service
metadata:
  name: external-app1
  labels:
    app: external-app1
spec:
  selector:
    app: external-app1
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP